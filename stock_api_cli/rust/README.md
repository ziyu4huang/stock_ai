# Rust Implementation (Future)

Placeholder for a Rust-based stock API connector.

Will reuse types from `crates/stock-core` and fetchers from `crates/stock-sources`.

## Planned Commands

```bash
cargo run -p stock-api-cli -- fetch SYMBOL --source yfinance
cargo run -p stock-api-cli -- quote SYMBOL --source yfinance
```
